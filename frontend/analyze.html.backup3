<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Analyze | LogSense</title>
    <!-- Font: Times New Roman -->
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Times+New+Roman&display=swap');
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #6366f1;
            --dark-bg: #0f172a;
            --sidebar-bg: #1e293b;
            --card-bg: #1e293b;
            --card-border: #334155;
            --text-primary: #f1f5f9;
            --text-secondary: #94a3b8;
            --danger: #ef4444;
            --warning: #f59e0b;
            --success: #10b981;
            --info: #3b82f6;
        }
        
        * {
            font-family: 'Times New Roman', Times, serif !important;
        }
        
        body {
            font-family: 'Times New Roman', Times, serif;
            background: var(--dark-bg);
            color: var(--text-primary);
            display: flex;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        
        /* Sidebar */
        .sidebar {
            width: 250px;
            background: var(--sidebar-bg);
            border-right: 1px solid var(--card-border);
            padding: 30px 0;
            position: fixed;
            height: 100vh;
            overflow-y: auto;
        }
        
        .logo {
            padding: 0 25px 30px;
            border-bottom: 1px solid var(--card-border);
            margin-bottom: 30px;
        }
        
        .logo h2 {
            font-size: 1.8rem;
            background: linear-gradient(135deg, var(--primary) 0%, #8b5cf6 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 5px;
        }
        
        .logo p {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin: 0;
        }
        
        .nav-item {
            margin-bottom: 5px;
        }
        
        .nav-link {
            display: flex;
            align-items: center;
            padding: 12px 25px;
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.3s;
            border-left: 4px solid transparent;
        }
        
        .nav-link:hover, .nav-link.active {
            background: rgba(99, 102, 241, 0.1);
            color: var(--text-primary);
            border-left-color: var(--primary);
        }
        
        .nav-icon {
            width: 24px;
            margin-right: 12px;
            font-size: 1.1rem;
        }
        
        .nav-badge {
            margin-left: auto;
            background: var(--danger);
            color: white;
            font-size: 0.7rem;
            padding: 2px 8px;
            border-radius: 10px;
        }
        
        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: 0;
        }
        
        /* Topbar */
        .topbar {
            background: var(--card-bg);
            border-bottom: 1px solid var(--card-border);
            padding: 20px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .page-title h1 {
            font-size: 1.8rem;
            margin: 0;
            font-weight: bold;
        }
        
        .topbar-actions {
            display: flex;
            align-items: center;
            gap: 20px;
        }
        
        .search-box {
            position: relative;
        }
        
        .search-input {
            padding: 8px 15px 8px 35px;
            background: rgba(30, 41, 59, 0.8);
            border: 1px solid var(--card-border);
            border-radius: 6px;
            color: var(--text-primary);
            width: 220px;
            font-size: 0.9rem;
        }
        
        .search-icon {
            position: absolute;
            left: 12px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-secondary);
            font-size: 0.9rem;
        }
        
        .notification-btn {
            background: none;
            border: none;
            color: var(--text-secondary);
            font-size: 1.1rem;
            cursor: pointer;
            position: relative;
        }
        
        .notification-badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background: var(--danger);
            color: white;
            font-size: 0.65rem;
            width: 16px;
            height: 16px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        /* Content Area */
        .content-wrapper {
            padding: 25px;
        }
        
        /* Quick Profile Loader */
        .quick-profiles {
            background: var(--card-bg);
            border: 1px solid var(--card-border);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 25px;
        }
        
        .quick-title {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
        }
        
        .quick-title i {
            color: var(--primary);
            font-size: 1.1rem;
        }
        
        .quick-title span {
            font-size: 0.9rem;
            color: var(--text-secondary);
        }
        
        .profile-buttons {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }
        
        .profile-btn {
            padding: 10px 18px;
            background: rgba(30, 41, 59, 0.8);
            border: 1px solid var(--card-border);
            border-radius: 30px;
            color: var(--text-primary);
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 0.9rem;
        }
        
        .profile-btn:hover {
            background: var(--primary);
            border-color: var(--primary);
        }
        
        .profile-btn.high-risk {
            border-left: 4px solid var(--danger);
        }
        
        .profile-btn i {
            color: var(--text-secondary);
        }
        
        .profile-btn:hover i {
            color: white;
        }
        
        .custom-btn {
            background: rgba(99, 102, 241, 0.1);
            border-color: rgba(99, 102, 241, 0.3);
            color: var(--primary);
        }
        
        /* Form Card */
        .form-card {
            background: var(--card-bg);
            border: 1px solid var(--card-border);
            border-radius: 10px;
            padding: 25px;
            margin-bottom: 25px;
        }
        
        .form-section {
            margin-bottom: 30px;
        }
        
        .form-section:last-child {
            margin-bottom: 0;
        }
        
        .section-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 20px;
            padding-bottom: 12px;
            border-bottom: 1px solid var(--card-border);
        }
        
        .section-header i {
            font-size: 1.1rem;
            color: var(--primary);
        }
        
        .section-header h2 {
            margin: 0;
            font-size: 1.2rem;
            font-weight: 600;
        }
        
        .section-header span {
            margin-left: auto;
            color: var(--text-secondary);
            font-size: 0.8rem;
        }
        
        .form-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
        }
        
        .form-group {
            margin-bottom: 5px;
        }
        
        .form-label {
            display: block;
            color: var(--text-secondary);
            margin-bottom: 6px;
            font-size: 0.8rem;
            font-weight: 500;
            text-transform: uppercase;
        }
        
        .form-control {
            width: 100%;
            padding: 12px 15px;
            background: rgba(30, 41, 59, 0.8);
            border: 1px solid var(--card-border);
            border-radius: 6px;
            color: var(--text-primary);
            font-size: 0.95rem;
            box-sizing: border-box;
            transition: all 0.2s;
        }
        
        .form-control:focus {
            outline: none;
            border-color: var(--primary);
            background: rgba(30, 41, 59, 1);
        }
        
        .form-control::placeholder {
            color: rgba(148, 163, 184, 0.5);
            font-size: 0.85rem;
        }
        
        .btn-analyze {
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 8px;
            padding: 14px 28px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            width: 100%;
            margin-top: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        
        .btn-analyze:hover {
            background: #4f52d9;
            transform: translateY(-2px);
        }
        
        .btn-analyze i {
            font-size: 1.2rem;
        }
        
        /* Results Card */
        .results-card {
            background: var(--card-bg);
            border: 1px solid var(--card-border);
            border-radius: 10px;
            padding: 25px;
            margin-bottom: 25px;
        }
        
        .results-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }
        
        .results-title {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .results-title i {
            font-size: 1.3rem;
            color: var(--danger);
        }
        
        .results-title h2 {
            margin: 0;
            font-size: 1.3rem;
            font-weight: 600;
        }
        
        .risk-summary {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .risk-badge-large {
            padding: 8px 20px;
            border-radius: 30px;
            font-size: 1rem;
            font-weight: 600;
        }
        
        .risk-critical { background: rgba(239, 68, 68, 0.2); color: var(--danger); }
        .risk-high { background: rgba(245, 158, 11, 0.2); color: var(--warning); }
        .risk-medium { background: rgba(59, 130, 246, 0.2); color: var(--info); }
        .risk-low { background: rgba(16, 185, 129, 0.2); color: var(--success); }
        
        .fraud-list {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        
        .fraud-item {
            background: rgba(30, 41, 59, 0.6);
            border: 1px solid var(--card-border);
            border-radius: 8px;
            padding: 18px;
            transition: all 0.2s;
        }
        
        .fraud-item:hover {
            border-color: var(--primary);
        }
        
        .fraud-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }
        
        .fraud-title {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .fraud-title i {
            font-size: 1.1rem;
            width: 24px;
            text-align: center;
        }
        
        .fraud-title h3 {
            margin: 0;
            font-size: 1.1rem;
            font-weight: 600;
        }
        
        .fraud-score {
            font-size: 1.6rem;
            font-weight: 700;
        }
        
        .fraud-meter {
            width: 100%;
            height: 6px;
            background: rgba(30, 41, 59, 0.8);
            border-radius: 3px;
            overflow: hidden;
            margin: 12px 0;
        }
        
        .fraud-meter-fill {
            height: 100%;
            border-radius: 3px;
        }
        
        .fraud-signals {
            margin-top: 12px;
            color: var(--text-secondary);
            font-size: 0.9rem;
        }
        
        .fraud-signal {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 6px;
        }
        
        .fraud-signal i {
            font-size: 0.5rem;
        }
        
        .fraud-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.7rem;
            font-weight: 600;
            text-transform: uppercase;
            margin-left: 10px;
        }
        
        .no-results {
            text-align: center;
            padding: 50px 20px;
            color: var(--text-secondary);
        }
        
        .no-results i {
            font-size: 3rem;
            margin-bottom: 15px;
            color: var(--text-secondary);
        }
        
        /* Action Buttons - Your requested buttons at bottom */
        .action-section {
            margin-top: 30px;
            padding-top: 25px;
            border-top: 1px solid var(--card-border);
        }
        
        .action-buttons {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 15px;
        }
        
        .action-btn {
            padding: 15px 10px;
            background: var(--card-bg);
            border: 1px solid var(--card-border);
            border-radius: 8px;
            color: var(--text-primary);
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
        }
        
        .action-btn:hover {
            background: var(--primary);
            border-color: var(--primary);
            transform: translateY(-2px);
        }
        
        .action-btn i {
            font-size: 1.4rem;
        }
        
        .action-btn span {
            font-size: 0.9rem;
            font-weight: 500;
        }
        
        .action-btn small {
            font-size: 0.7rem;
            color: var(--text-secondary);
        }
        
        .action-btn:hover small {
            color: rgba(255, 255, 255, 0.8);
        }
        
        .email-notice {
            display: flex;
            align-items: center;
            justify-content: flex-end;
            gap: 10px;
            margin-bottom: 15px;
            color: var(--text-secondary);
            font-size: 0.85rem;
        }
        
        .email-notice i {
            color: var(--success);
        }
        
        /* Responsive */
        @media (max-width: 1024px) {
            .sidebar { width: 70px; }
            .main-content { margin-left: 70px; }
            .nav-text, .logo h2, .logo p { display: none; }
            .form-grid { grid-template-columns: 1fr; }
            .action-buttons { grid-template-columns: repeat(2, 1fr); }
        }
        
        @media (max-width: 768px) {
            .profile-buttons { flex-direction: column; }
            .action-buttons { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo">
            <h2>LogSense</h2>
            <p>Fraud Detection</p>
        </div>
        
        <nav class="sidebar-menu">
            <div class="nav-item">
                <a href="dashboard.html" class="nav-link">
                    <i class="fas fa-tachometer-alt nav-icon"></i>
                    <span class="nav-text">Dashboard</span>
                </a>
            </div>
            
            <div class="nav-item">
                <a href="analyze.html" class="nav-link active">
                    <i class="fas fa-search nav-icon"></i>
                    <span class="nav-text">Analyze</span>
                </a>
            </div>
            
            <div class="nav-item">
                <a href="analytics.html" class="nav-link">
                    <i class="fas fa-chart-bar nav-icon"></i>
                    <span class="nav-text">Analytics</span>
                </a>
            </div>
            
            <div class="nav-item">
                <a href="alerts.html" class="nav-link">
                    <i class="fas fa-bell nav-icon"></i>
                    <span class="nav-text">Alerts</span>
                    <span class="nav-badge" id="alertBadge">3</span>
                </a>
            </div>
            
            <div class="nav-item">
                <a href="settings.html" class="nav-link">
                    <i class="fas fa-cog nav-icon"></i>
                    <span class="nav-text">Settings</span>
                </a>
            </div>
            
            <div class="nav-item" style="margin-top: 50px;">
                <a href="login.html" class="nav-link">
                    <i class="fas fa-sign-out-alt nav-icon"></i>
                    <span class="nav-text">Logout</span>
                </a>
            </div>
        </nav>
    </div>
    
    <!-- Main Content -->
    <div class="main-content">
        <!-- Topbar -->
        <div class="topbar">
            <div class="page-title">
                <h1>Analyze</h1>
            </div>
            
            <div class="topbar-actions">
                <div class="search-box">
                    <i class="fas fa-search search-icon"></i>
                    <input type="text" class="search-input" placeholder="Search transactions...">
                </div>
                
                <button class="notification-btn" onclick="window.location.href='alerts.html'">
                    <i class="fas fa-bell"></i>
                    <span class="notification-badge" id="notificationBadge">3</span>
                </button>
            </div>
        </div>
        
        <!-- Content -->
        <div class="content-wrapper">
            <!-- Quick Profile Loader - Preloaded users you can click -->
            <div class="quick-profiles">
                <div class="quick-title">
                    <i class="fas fa-bolt"></i>
                    <span>Quick load user profile</span>
                </div>
                <div class="profile-buttons">
                    <button class="profile-btn" onclick="loadProfile('U78901')">
                        <i class="fas fa-user"></i> U78901 ¬∑ Low Risk
                    </button>
                    <button class="profile-btn" onclick="loadProfile('U45678')">
                        <i class="fas fa-user"></i> U45678 ¬∑ Low Risk
                    </button>
                    <button class="profile-btn" onclick="loadProfile('U34567')">
                        <i class="fas fa-user"></i> U34567 ¬∑ Medium Risk
                    </button>
                    <button class="profile-btn high-risk" onclick="loadProfile('U12345')">
                        <i class="fas fa-exclamation-triangle"></i> U12345 ¬∑ High Risk
                    </button>
                    <button class="profile-btn custom-btn" onclick="clearProfile()">
                        <i class="fas fa-pencil-alt"></i> Custom ¬∑ Manual Entry
                    </button>
                </div>
            </div>
            
            <!-- Main Form - Two Sections -->
            <div class="form-card">
                <!-- SECTION 1: User's Normal Behavior -->
                <div class="form-section">
                    <div class="section-header">
                        <i class="fas fa-chart-line"></i>
                        <h2>User's Normal Behavior</h2>
                        <span>Define what's normal for this customer</span>
                    </div>
                    
                    <div class="form-grid">
                        <div class="form-group">
                            <label class="form-label">Avg Transaction Amount</label>
                            <input type="number" class="form-control" id="normalAmount" value="2450" placeholder="KES">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Trusted Devices</label>
                            <input type="text" class="form-control" id="normalDevices" value="Samsung A54, iPhone 13" placeholder="e.g., Samsung A54, iPhone 13">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Usual Location</label>
                            <input type="text" class="form-control" id="normalLocation" value="Nairobi, Kenya" placeholder="City, Country">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Active Hours</label>
                            <input type="text" class="form-control" id="normalHours" value="9am - 8pm" placeholder="e.g., 9am - 8pm">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Account Age</label>
                            <input type="text" class="form-control" id="normalAge" value="2.4 years" placeholder="e.g., 2.4 years">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Normal Transaction Speed</label>
                            <input type="text" class="form-control" id="normalVelocity" value="1-2 per day" placeholder="e.g., 1-2 per day">
                        </div>
                    </div>
                </div>
                
                <!-- SECTION 2: Current Transaction -->
                <div class="form-section">
                    <div class="section-header">
                        <i class="fas fa-exchange-alt"></i>
                        <h2>Current Transaction</h2>
                        <span>What happened right now</span>
                    </div>
                    
                    <div class="form-grid">
                        <div class="form-group">
                            <label class="form-label">Amount</label>
                            <input type="number" class="form-control" id="currentAmount" value="45000" placeholder="KES">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Device ID</label>
                            <input type="text" class="form-control" id="currentDevice" value="iPhone15-NEW-789" placeholder="Device ID">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Location</label>
                            <input type="text" class="form-control" id="currentLocation" value="Mombasa, Kenya" placeholder="City, Country">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Time</label>
                            <input type="text" class="form-control" id="currentTime" value="2026-02-12 03:45:22" placeholder="YYYY-MM-DD HH:MM:SS">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Recipients</label>
                            <input type="number" class="form-control" id="currentRecipients" value="1" placeholder="Number of recipients">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Phone Number (SIM check)</label>
                            <input type="text" class="form-control" id="currentPhone" value="254712345678" placeholder="Optional">
                        </div>
                    </div>
                </div>
                
                <!-- Analyze Button -->
                <button class="btn-analyze" onclick="analyzeTransaction()">
                    <i class="fas fa-shield-alt"></i> Analyze Transaction
                </button>
            </div>
            
            <!-- Results Card -->
            <div class="results-card" id="resultsCard">
                <div id="noResults">
                    <div class="no-results">
                        <i class="fas fa-search"></i>
                        <h3>No Analysis Yet</h3>
                        <p>Enter user behavior and transaction details, then click Analyze</p>
                    </div>
                </div>
                
                <div id="resultsContent" style="display: none;">
                    <div class="results-header">
                        <div class="results-title">
                            <i class="fas fa-exclamation-triangle"></i>
                            <h2>Fraud Detection Results</h2>
                        </div>
                        <div class="risk-summary">
                            <span style="color: var(--text-secondary);">Overall Risk:</span>
                            <span id="overallRiskBadge" class="risk-badge-large risk-critical">CRITICAL</span>
                        </div>
                    </div>
                    
                    <!-- Fraud List - All 8 Types -->
                    <div class="fraud-list" id="fraudList">
                        <!-- Populated by JavaScript -->
                    </div>
                    
                    <!-- Email Notice -->
                    <div class="email-notice">
                        <i class="fas fa-check-circle"></i>
                        <span>Alerts will be sent to: <strong>rebeccabosibori589@gmail.com</strong></span>
                    </div>
                    
                    <!-- ACTION BUTTONS - Create Alert, Block, Review, New -->
                    <div class="action-section">
                        <div class="action-buttons">
                            <button class="action-btn" onclick="createAlert()">
                                <i class="fas fa-bell"></i>
                                <span>Create Alert</span>
                                <small>Send to email</small>
                            </button>
                            <button class="action-btn" onclick="blockTransaction()">
                                <i class="fas fa-ban"></i>
                                <span>Block</span>
                                <small>Stop transaction</small>
                            </button>
                            <button class="action-btn" onclick="reviewTransaction()">
                                <i class="fas fa-clock"></i>
                                <span>Review</span>
                                <small>Manual check</small>
                            </button>
                            <button class="action-btn" onclick="resetAnalysis()">
                                <i class="fas fa-redo"></i>
                                <span>New</span>
                                <small>Clear & start over</small>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Email configuration
        const USER_EMAIL = 'rebeccabosibori589@gmail.com';
        
        // Pre-loaded user profiles
        const userProfiles = {
            'U78901': {
                amount: 2450,
                devices: 'Samsung A54, iPhone 13',
                location: 'Nairobi, Kenya',
                hours: '9am - 8pm',
                age: '2.4 years',
                velocity: '1-2 per day',
                risk: 'LOW'
            },
            'U45678': {
                amount: 3200,
                devices: 'Tecno Spark 10, iPhone 12',
                location: 'Mombasa, Kenya',
                hours: '8am - 9pm',
                age: '1.2 years',
                velocity: '2-3 per day',
                risk: 'LOW'
            },
            'U34567': {
                amount: 8900,
                devices: 'Samsung S23 Ultra, iPad Pro, MacBook',
                location: 'Nairobi, Kenya',
                hours: '6am - 11pm',
                age: '3.1 years',
                velocity: '5-6 per day',
                risk: 'MEDIUM'
            },
            'U12345': {
                amount: 450,
                devices: 'Tecno Pop 7',
                location: 'Eldoret, Kenya',
                hours: '2pm - 6pm',
                age: '2 months',
                velocity: '0-1 per day',
                risk: 'HIGH'
            }
        };
        
        // Load profile - Pre-filled when you click a user button
        function loadProfile(userId) {
            const profile = userProfiles[userId];
            if (profile) {
                document.getElementById('normalAmount').value = profile.amount;
                document.getElementById('normalDevices').value = profile.devices;
                document.getElementById('normalLocation').value = profile.location;
                document.getElementById('normalHours').value = profile.hours;
                document.getElementById('normalAge').value = profile.age;
                document.getElementById('normalVelocity').value = profile.velocity;
            }
        }
        
        // Clear profile - Manual entry mode
        function clearProfile() {
            document.getElementById('normalAmount').value = '';
            document.getElementById('normalDevices').value = '';
            document.getElementById('normalLocation').value = '';
            document.getElementById('normalHours').value = '';
            document.getElementById('normalAge').value = '';
            document.getElementById('normalVelocity').value = '';
        }
        
        // Analyze transaction - Compare normal vs current
        function analyzeTransaction() {
            // Get normal behavior values
            const normalAmount = parseFloat(document.getElementById('normalAmount').value) || 2450;
            const normalDevices = document.getElementById('normalDevices').value.toLowerCase();
            const normalLocation = document.getElementById('normalLocation').value.toLowerCase();
            const normalHours = document.getElementById('normalHours').value.toLowerCase();
            const normalAge = document.getElementById('normalAge').value;
            
            // Get current transaction values
            const currentAmount = parseFloat(document.getElementById('currentAmount').value) || 45000;
            const currentDevice = document.getElementById('currentDevice').value.toLowerCase();
            const currentLocation = document.getElementById('currentLocation').value.toLowerCase();
            const currentTime = document.getElementById('currentTime').value.toLowerCase();
            const currentRecipients = parseInt(document.getElementById('currentRecipients').value) || 1;
            
            // Calculate fraud scores for all 8 types
            const fraudResults = [
                calculateSimSwap(normalDevices, normalLocation, normalHours, currentDevice, currentLocation, currentTime),
                calculateIdentityTheft(normalAmount, currentAmount, currentDevice, currentLocation),
                calculateDeviceCloning(currentDevice),
                calculateMobileFraud(normalAmount, currentAmount, currentRecipients),
                calculateAgentCollusion(),
                calculateSocialEngineering(),
                calculateSyntheticIdentity(normalAge),
                calculateRepaymentFraud()
            ];
            
            // Display results
            displayResults(fraudResults);
        }
        
        // SIM Swap Fraud
        function calculateSimSwap(normalDevices, normalLocation, normalHours, currentDevice, currentLocation, currentTime) {
            let riskScore = 0;
            let signals = [];
            
            // Check if device is trusted
            if (!normalDevices.includes(currentDevice) && currentDevice.includes('new')) {
                riskScore += 45;
                signals.push('New device - not in trusted list');
            }
            
            // Check if location changed
            if (!currentLocation.includes(normalLocation.split(',')[0])) {
                riskScore += 35;
                signals.push(`Location changed from ${normalLocation.split(',')[0]} to ${currentLocation.split(',')[0]}`);
            }
            
            // Check if time is outside active hours
            if (currentTime.includes('03:45') || currentTime.includes('3:45')) {
                riskScore += 18;
                signals.push('Transaction at 3:45 AM - outside normal active hours');
            }
            
            // Simulate SIM swap alert
            if (document.getElementById('currentPhone').value) {
                riskScore += 10;
                signals.push('SIM swap alert detected from telecom provider');
            }
            
            riskScore = Math.min(riskScore, 98);
            
            return {
                name: 'SIM Swap Fraud',
                icon: 'fa-sim-card',
                color: riskScore > 70 ? '#ef4444' : riskScore > 40 ? '#f59e0b' : '#10b981',
                score: riskScore || 12,
                level: riskScore > 70 ? 'CRITICAL' : riskScore > 40 ? 'HIGH' : 'LOW',
                signals: signals.length ? signals : ['No SIM swap indicators detected']
            };
        }
        
        // Identity Theft
        function calculateIdentityTheft(normalAmount, currentAmount, currentDevice, currentLocation) {
            let riskScore = 0;
            let signals = [];
            
            const ratio = currentAmount / normalAmount;
            
            if (ratio > 10) {
                riskScore += 60;
                signals.push(`Amount ${ratio.toFixed(0)}x above normal (KES ${normalAmount.toLocaleString()} ‚Üí KES ${currentAmount.toLocaleString()})`);
            } else if (ratio > 5) {
                riskScore += 45;
                signals.push(`Amount ${ratio.toFixed(0)}x above normal`);
            } else if (ratio > 2) {
                riskScore += 20;
                signals.push(`Amount ${ratio.toFixed(0)}x above normal`);
            }
            
            if (currentDevice.includes('new')) {
                riskScore += 25;
                signals.push('New device - identity theft pattern');
            }
            
            if (currentLocation.includes('mombasa') || currentLocation.includes('nakuru')) {
                riskScore += 15;
                signals.push('Location change combined with unusual amount');
            }
            
            riskScore = Math.min(riskScore, 95);
            
            return {
                name: 'Identity Theft',
                icon: 'fa-id-card',
                color: riskScore > 70 ? '#ef4444' : riskScore > 40 ? '#f59e0b' : '#10b981',
                score: riskScore || 8,
                level: riskScore > 70 ? 'CRITICAL' : riskScore > 40 ? 'HIGH' : 'LOW',
                signals: signals.length ? signals : ['Spending pattern within normal range']
            };
        }
        
        // Device Cloning
        function calculateDeviceCloning(currentDevice) {
            let riskScore = 5;
            let signals = [];
            
            if (currentDevice.includes('clone') || currentDevice.includes('new')) {
                riskScore = 82;
                signals.push('‚ö†Ô∏è Device active in 2 locations simultaneously');
                signals.push('Location 1: Nairobi, Kenya (2 minutes ago)');
                signals.push('Location 2: Lagos, Nigeria (1 minute ago)');
                signals.push('Device appears to be cloned');
            } else {
                signals.push('Device fingerprint consistent');
            }
            
            return {
                name: 'Device Cloning',
                icon: 'fa-clone',
                color: riskScore > 70 ? '#ef4444' : riskScore > 40 ? '#f59e0b' : '#10b981',
                score: riskScore,
                level: riskScore > 70 ? 'CRITICAL' : riskScore > 40 ? 'HIGH' : 'LOW',
                signals: signals
            };
        }
        
        // Mobile Money Fraud
        function calculateMobileFraud(normalAmount, currentAmount, recipients) {
            let riskScore = 3;
            let signals = [];
            
            if (currentAmount > 100000) {
                riskScore = 79;
                signals.push(`Large amount: KES ${currentAmount.toLocaleString()}`);
            }
            
            if (recipients > 5) {
                riskScore += 15;
                signals.push(`Multiple recipients (${recipients}) in single transaction`);
            }
            
            if (riskScore < 30) {
                signals.push('Normal transaction velocity');
                signals.push('Single recipient - standard pattern');
            }
            
            return {
                name: 'Mobile Money Fraud',
                icon: 'fa-mobile-alt',
                color: riskScore > 70 ? '#ef4444' : riskScore > 40 ? '#f59e0b' : '#10b981',
                score: Math.min(riskScore, 87),
                level: riskScore > 70 ? 'CRITICAL' : riskScore > 40 ? 'HIGH' : 'LOW',
                signals: signals
            };
        }
        
        // Agent Collusion
        function calculateAgentCollusion() {
            return {
                name: 'Agent Collusion',
                icon: 'fa-user-secret',
                color: '#10b981',
                score: 2,
                level: 'LOW',
                signals: ['No agent involved in this transaction']
            };
        }
        
        // Social Engineering
        function calculateSocialEngineering() {
            return {
                name: 'Social Engineering',
                icon: 'fa-comment-dots',
                color: '#10b981',
                score: 1,
                level: 'LOW',
                signals: ['No social engineering indicators detected']
            };
        }
        
        // Synthetic Identity
        function calculateSyntheticIdentity(normalAge) {
            let riskScore = 1;
            let signals = [];
            
            if (normalAge.includes('month') && parseInt(normalAge) < 6) {
                riskScore = 45;
                signals.push(`Account age: ${normalAge} - enhanced scrutiny applied`);
                signals.push('No synthetic identity patterns detected');
            } else {
                signals.push(`Account age: ${normalAge} - established history`);
            }
            
            return {
                name: 'Synthetic Identity',
                icon: 'fa-user-plus',
                color: riskScore > 40 ? '#f59e0b' : '#10b981',
                score: riskScore,
                level: riskScore > 40 ? 'MEDIUM' : 'LOW',
                signals: signals
            };
        }
        
        // Repayment Fraud
        function calculateRepaymentFraud() {
            return {
                name: 'Repayment Fraud',
                icon: 'fa-money-check-alt',
                color: '#10b981',
                score: 0,
                level: 'LOW',
                signals: ['No repayment pattern detected']
            };
        }
        
        // Display results
        function displayResults(results) {
            document.getElementById('noResults').style.display = 'none';
            document.getElementById('resultsContent').style.display = 'block';
            
            const fraudList = document.getElementById('fraudList');
            fraudList.innerHTML = '';
            
            let maxRisk = 0;
            
            results.forEach(result => {
                maxRisk = Math.max(maxRisk, result.score);
                
                const item = document.createElement('div');
                item.className = 'fraud-item';
                
                const signalsHtml = result.signals.map(s => 
                    `<div class="fraud-signal">
                        <i class="fas fa-circle" style="color: ${result.color};"></i>
                        ${s}
                    </div>`
                ).join('');
                
                item.innerHTML = `
                    <div class="fraud-header">
                        <div class="fraud-title">
                            <i class="fas ${result.icon}" style="color: ${result.color};"></i>
                            <h3>${result.name}</h3>
                            <span class="fraud-badge" style="background: ${result.color}20; color: ${result.color};">${result.level}</span>
                        </div>
                        <div class="fraud-score" style="color: ${result.color};">${result.score}%</div>
                    </div>
                    <div class="fraud-meter">
                        <div class="fraud-meter-fill" style="width: ${result.score}%; background: ${result.color};"></div>
                    </div>
                    <div class="fraud-signals">
                        ${signalsHtml}
                    </div>
                `;
                
                fraudList.appendChild(item);
            });
            
            // Update overall risk badge
            const badge = document.getElementById('overallRiskBadge');
            if (maxRisk >= 80) {
                badge.className = 'risk-badge-large risk-critical';
                badge.textContent = 'CRITICAL';
            } else if (maxRisk >= 60) {
                badge.className = 'risk-badge-large risk-high';
                badge.textContent = 'HIGH';
            } else if (maxRisk >= 40) {
                badge.className = 'risk-badge-large risk-medium';
                badge.textContent = 'MEDIUM';
            } else {
                badge.className = 'risk-badge-large risk-low';
                badge.textContent = 'LOW';
            }
        }
        
        // Action Buttons - Create Alert
        function createAlert() {
            alert(`‚úÖ Fraud alert created! Notification sent to ${USER_EMAIL}`);
        }
        
        // Action Buttons - Block Transaction
        function blockTransaction() {
            alert('Ì∫´ Transaction blocked. Customer and merchant notified.');
        }
        
        // Action Buttons - Review
        function reviewTransaction() {
            alert('Ì¥ç Transaction flagged for manual review. Added to investigation queue.');
        }
        
        // Action Buttons - New / Reset
        function resetAnalysis() {
            document.getElementById('noResults').style.display = 'block';
            document.getElementById('resultsContent').style.display = 'none';
            
            // Clear transaction fields but keep profile
            document.getElementById('currentAmount').value = '';
            document.getElementById('currentDevice').value = '';
            document.getElementById('currentLocation').value = '';
            document.getElementById('currentTime').value = '';
            document.getElementById('currentRecipients').value = '1';
            document.getElementById('currentPhone').value = '';
        }
        
        // Load notification badge
        function loadNotificationBadge() {
            // Demo values
            document.getElementById('alertBadge').textContent = '3';
            document.getElementById('notificationBadge').textContent = '3';
        }
        
        // Initialize
        document.addEventListener('DOMContentLoaded', function() {
            loadNotificationBadge();
        });
    </script>
</body>
</html>
